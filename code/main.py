import time
from sentral import Sentral

"""
s = Sentral()
def run():
    while True:
        if s.incoming_call():
            s.button_was_pressed = False
            print("--------------------------------")
            for item in s.hooks:
                for key in item:
                    print(f"{key} : {item[key]}")
            s.reset_hooks()

        time.sleep(0.2)


while True:
    print(s.hook_count)
    time.sleep(1)

"""
